# Static Folder

This folder will contain static assets (if required) for the TrailService micro-service.

### Planned Contents:
1. HTML files: For web interface (if any).
2. CSS files: For styling the web interface (if required).
3. JavaScript files: For client-side interactions (if required).

### Purpose:
The `static` folder is optional and can store static assets if the micro-service includes a user interface.
